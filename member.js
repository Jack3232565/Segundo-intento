for (let function for (let index = 0; index < array.length; index++) {
    const element = array[index];
    name
}(params) {
    index
} = 0; function name(params) {
    index
} < array.length; function name(params) {
    index
}++) {
    const element = array[function name(params) {
        index
    }];
    function function do {
        name
    } while (condition);(params) {
        skillsMember
    }()
}